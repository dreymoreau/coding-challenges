// Title: Convert a string to a number! 

// declare if statement
// return string as a number by wrapping the parameter with parseFloat()

const stringToNumber = function(str){
    if(str) {
        return parseFloat(str)
    }
    return null;
  }